# CSS Loading Animation

A creative and stylish text loading animation built with HTML and pure CSS.

## 🚀 Description

This project demonstrates a "filling text" animation effect. The text "Creative..." is displayed with a stroke outline, and a bright green fill color animates from left to right, simulating a loading progress or a creative reveal.

## ✨ Features

- **Pure CSS Animation**: No JavaScript required.
- **Text Stroke Effect**: Uses `-webkit-text-stroke` for a modern outlined look.
- **Data Attribute Usage**: Utilizes `data-text` attribute for the overlay content.
- **Responsive Design**: Centered layout using Flexbox.
- **Infinite Loop**: The animation runs continuously.

## 🛠️ Technologies Used

- **HTML5**
- **CSS3** (Flexbox, Keyframes, Pseudo-elements)
- **Google Fonts** (Poppins)

## 📖 How to Run

1. Clone or download the repository.
2. Open the `index.html` file in any modern web browser.

## 🎨 Customization

To change the text, update the `h2` element in `index.html`:

```html
<h2 data-text="Your Text Here">Your Text Here</h2>
```

Ensure both the inner text and the `data-text` attribute match for the effect to work correctly.
